const gcd = function gcd(a, b) {
	// write your code here
    if ( ! b) {
        return a;
    }

	return gcd(b, a % b);
};

const a = 2154
const b = 45

console.log("The GCD of " + a + " ", b + " is " + gcd(a, b));
